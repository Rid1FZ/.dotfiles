;;; command-log-mode.el --- Config For `command-log-mode' package -*- lexical-binding: t -*-

;;; Commentary:

;;; Code:

(use-package command-log-mode
  :ensure t
  :defer t

  :config
  (global-command-log-mode +1))

;;; command-log-mode.el ends here
