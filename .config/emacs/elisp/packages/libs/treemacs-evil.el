;;; treemacs-evil.el --- Config For `treemacs-evil' package -*- lexical-binding: t -*-

;;; Commentary:

;;; Code:

(use-package treemacs-evil
  :ensure t

  :after
  (treemacs evil))

;;; treemacs-evil.el ends here
