return {
	ensure_installed = { "lua", "vim", "vimdoc", "markdown_inline", "comment" },
	auto_install = true,
	highlight = {
		enable = true,
		use_languagetree = true,
	},

	indent = { enable = true },
}
